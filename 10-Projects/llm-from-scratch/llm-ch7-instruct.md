# llm-ch7-instruct

[[llm-from-scratch]] 마스터 플랜의 **Phase 7 / 7장** 정본 노트.
교재: Sebastian Raschka, *밑바닥부터 만들면서 배우는 LLM* — 7장. 지시를 따르도록 미세 튜닝하기.

## 개요

instruction 데이터셋과 프롬프트 템플릿으로 모델이 지시를 따르도록 미세 튜닝하고, 정성·자동 평가로 마무리하는 마지막 장.

## 체크리스트

- [x] 7.1 instruction 데이터셋 구성 — 다운로드+분할 (`instruction_dataset_finetune.py`, rasbt 1100건)
- [x] 7.2 입력 포맷팅(프롬프트 템플릿) `format_input` 적용 (Alpaca식)
- [x] 7.3 `InstructionDataset`(토큰화) + **사용자 정의 콜레이트 함수**(배치 시 패딩·타깃·-100) — draft 1·2 + `custom_collate_fn`, 토이 예제 loss 확인, DataLoader 3종 배치 shape 검증까지 완료
- [~] 7.4 instruction fine-tuning 학습 — `llm_finetuning.py`: gpt2-medium(355M) 로컬 로드까지 완료 (hf_weight_adapter 경유)
- [ ] 7.5 정성 평가 + 간단한 자동 평가 루프
- [ ] 7.6 회고 — 사전훈련 vs 분류 FT vs 지시 FT의 차이 노트화

## 학습 메모 (실습으로 익힌 것)

### 데이터 준비 (`instruction_dataset_finetune.py`)
- 다운로드: rasbt 레포 `instruction-data.json`(1100건). `import urllib.request` 필요 — `import urllib`만 하면 `urllib.request` 미로드(AttributeError). 실제 겪음.
- `format_input`: Alpaca식 프롬프트 템플릿("Below is an instruction..." + `### Instruction` + 선택적 `### Input`). 응답은 `### Response`로 별도 결합.
- 분할: 단순 **순서 슬라이싱** train 85%/test 10%/val 나머지. ch6은 `random_split`(섞음) — ch7은 순차 분할(데이터가 이미 다양). val은 `[...:]`로 나머지 전부(int 버림 누락 방지).

### 콜레이트 함수 = 배치 시점 처리 (단계 2.x) — ch6 DataLoader로 부족
- 데이터 준비(다운로드·format·분할)는 **콜레이트 전 단계**. `InstructionDataset`(토큰화)이 단계 1, **콜레이트 함수**가 단계 2.x.
- ch6은 입력만 패딩하면 끝(라벨=숫자). ch7은 입력·타깃 둘 다 자유 텍스트라 콜레이트에서 ①배치 내 최장 길이 패딩 ②**타깃=입력 한 토큰 이동** ③패딩 자리 **-100**(손실 제외)까지 직접 처리 → 그래서 사용자 정의 콜레이트.

### 타깃 = 입력 한 토큰 이동 = 다음 토큰 예측 (사전훈련과 동일)
- `입력[A B C D]` → `타깃[B C D E]`(한 칸 밀림). 각 위치에서 "바로 다음 토큰" 맞히기 = ch2 슬라이딩 윈도·ch5 사전훈련과 **같은 메커니즘**.
- "한 토큰 이동"(학습 방식) ≠ "GPT가 토큰 하나씩 다룸"(causal/자기회귀 구조)이지만 **원인-결과**: 이렇게 학습해서 → 자기회귀 생성이 가능.

### 분류 FT(6장) vs 명령어 FT(7장)
| | 분류(ch6) | 명령어(ch7) |
|---|---|---|
| 출력 | 고정 클래스(0/1) | 자유 텍스트 생성 |
| head | 768→2 **교체** | 768→50257 **유지** |
| 쓰는 출력 | 마지막 토큰 argmax | 토큰 하나씩(자기회귀) |
| 손실 | 마지막 토큰 CE | 모든 위치 다음 토큰 CE |
| 비유 | 객관식 | 서술형 |

### 로짓 = "어휘사전 각 칸의 점수 벡터" (2026-07-02 Q&A로 체득)
- **어휘사전 = 번호 붙은 토큰 명단**(0~50256, 고정·숫자 없음). **로짓 = 모델이 그 명단 전원에게 매긴 "다음에 올 것 같은 정도" 점수**. 명단은 로짓의 **틀**(칸 수·칸의 의미)만 제공, **값은 모델 계산**(`model(input_batch)`)이 만듦 — heads.py:123.
  - 비유: 어휘사전 = 선거 후보 명부(번호만), 로짓 = 득표수(투표 = 모델 계산, 매번 다름).
- 로짓 표의 두 축: **행 = 위치**("문장의 몇 번째 자리에서 다음은?"이라는 질문, 입력 길이만큼), **열 = 칸**(어휘사전 후보 번호, vocab_size로 고정). 숫자 하나 = "위치 △에서 후보 ○번이 올 점수".
  - `torch.tensor([[a,b],[c,d]])`에서 **안쪽 리스트 개수 = 위치 수, 리스트 속 숫자 개수 = 칸 수**. 토이 예제 = 위치 2 × 칸 2 미니 세계 (별도 '명단' 객체는 없음 — cross_entropy가 행 폭으로 후보 수 추론).
- 모델 내부에서 로짓이 태어나는 곳 = **마지막 출력 층**: 토큰 ID → 임베딩(768차원) → 트랜스포머 12블록 → **Linear 768→50257**이 위치마다 점수 벡터 생성.

### target = "정답 칸 번호표" + cross_entropy 채점 (loss 1.1269 분해)
- `targets = [0, 1]`은 **좌표가 아니라 답안지 두 줄**: "위치 1의 정답 = 0번 칸, 위치 2의 정답 = 1번 칸". target 길이 = 로짓 행 수, 값 = 정답 열 번호. (처음에 `[행0, 열1]` 좌표로 오독했음.)
- **예측 ≠ 정답**: 예측 = 로짓 최고점 칸(모델 생각), 정답 = 데이터가 정한 칸(target). 둘의 어긋남이 벌점의 근원. 토이 예제 위치 1이 일부러 "틀린 경우"(예측 칸1, 정답 칸0).
- cross_entropy 3단계: ①softmax로 확률화 ②**정답 칸의 확률만** 꺼냄 ③−log 벌점. loss **1.1269** = (틀린 위치 2.1269 + 맞힌 위치 0.1269) / 2. loss=0 = 정답에 확률 100%. **MLflow의 train_loss/val_loss가 정확히 이 계산.**
- **-100 = ignore_index**: target이 -100인 위치는 채점에서 완전 제외. 패딩 자리에 적용(패딩 맞히기 학습 방지). 행을 추가해도 -100이면 loss 불변(1.1269 유지)으로 확인.
- `allowed_special={"<|endoftext|>"}`: tiktoken은 특수 토큰 문자열을 기본 **거부**(데이터에 우연히 섞인 걸 진짜 신호로 오해 방지) → 의도적 사용임을 명시해야 ID 50256 하나로 인코딩. ch7 패딩 토큰으로 사용.

### 겪은 버그 3종 (콜레이트·Dataset 작성 중)
- `stack expects a non-empty TensorList` → **엉뚱한 리스트에 append**(`inputs_lst.append(targets)`). 교훈: 빈 리스트 stack 에러는 그 리스트에 append하는 곳을 역추적.
- `encoded_tests` 오타(→`encoded_texts`), `__getitem__`/`__len__`이 **`__init__` 안에 중첩**(들여쓰기) — 정의 시점엔 안 터지고 DataLoader가 부를 때 터지는 지연 폭탄류.

### 연습문제 7.2 — 지시 마스킹 (구현 요점)
- 목표: **채점 범위를 응답 구간으로 좁히기** — 패딩에 쓴 -100을 지시문+입력 구간에도 적용 (지시문 암송 학습 방지).
- 구현 3곳: ①Dataset이 `len(tokenizer.encode(instruction_plus_input))` 저장 ②`__getitem__`이 (길이, 토큰열) 반환 + 콜레이트가 쌍으로 언패킹 ③패딩 마스킹 뒤 `targets[:instruction_length-1] = -100`.
  - **-1인 이유**: target은 한 칸 밀린 것 → `targets[L-1]` = **응답 첫 토큰** 맞히기 → 남겨야 함.
- 단, 2026-07 현재 "마스킹이 무조건 정답" 아님 — 이 데이터(1,100건·짧은 응답)는 비마스킹 우세 조건. **두 방식을 MLflow run으로 비교가 올바른 과제 자세** → 상세: [[../../30-References/instruction-masking-trend]].

### 로더 shape 확인의 의미 — 학습 전 싼 검증 (2026-07-02 Q&A)
- 확인 루프(`for inputs, targets in train_loader: print(shape)`)는 **비싼 단계(7.4 학습) 전에 콜레이트가 약속대로 일했는지 모양만으로 검사**하는 관례. `[8, 61]` 한 줄에서 약속 3개 확인:
  ① 첫 숫자 8 = batch_size — Dataset↔DataLoader 연결 정상 (`__getitem__` 들여쓰기 버그의 검출기이기도 했음)
  ② inputs·targets 모양 동일 — "한 칸 밀기" 쌍 성립 (`padded[:-1]`/`[1:]`)
  ③ **두 번째 숫자가 배치마다 다름(61~91) = 동적 패딩 작동의 지문**. 전부 같으면 고정 패딩 의심.
- 단 shape는 **필요조건이지 충분조건 아님**(-100 위치 오류는 모양에 안 나타남) → 교재의 2단계 검증: 토이 배치로 **내용**을 눈으로, 실데이터로 **모양**을.

### 동적 패딩(배치별 폭)이 장점인 이유
- 기본 콜레이트는 길이 제각각인 샘플을 stack 못 해 에러 → 선택지: ①전체 고정 길이로 미리 패딩(ch6 방식) ②일괄 자르기(데이터 손실) ③**배치 시점에 그 배치 최장으로 패딩(커스텀 콜레이트)**.
- ③이 되는 이유 = 패딩 시점: Dataset 단계선 배치 멤버를 몰라 최악(전체 최장) 대비 필요, **콜레이트는 8개 확정 후라 "이 중 최장"이라는 최소 기준 가능**. shuffle로 매 에폭 조합이 바뀌어도 매번 그 자리에서 재계산.
- 왜 고정 패딩이 낭비인가: **모델은 패딩 토큰도 진짜 토큰과 같은 비용으로 계산**(임베딩→12블록→로짓 전부. -100은 마지막 채점만 제외). 최장 61 배치를 1024로 채우면 의미 있는 계산이 6% 미만. loss는 어느 쪽이든 동일 → **"같은 결과를 더 싸게"가 전부, 정확도와 무관**.
- 두 파라미터의 역할 분리: `batch_max_length` = **바닥 맞추기**(배치마다 유동) / `allowed_max_length=1024` = **천장 자르기**(context_length 초과 안전장치, 이 데이터셋에선 미발동).

### 데이터 계층: 샘플 → 분할 → 배치 → 토큰 ID
- **instruction 샘플 = json의 낱개 1건** = dict{instruction, input, output} = "문제-모범답안 1세트". train/val/test_data는 샘플들의 **묶음**(935/55/110건) — 샘플은 train_data의 구성 알갱이.
- 배치 멤버 = 샘플 1건을 [템플릿 결합 → `tokenizer.encode`]한 **토큰 ID 목록** (문장 아님, 지시+입력+응답 통짜; 콜레이트 도착 시점엔 이미 숫자). 토이 배치 `[0,1,2,3,4]`류가 이것의 축소판.
- **토큰 ID = 어휘사전 칸 번호** — 등장한 숫자가 전부 한 체계: encode 결과·inputs·targets(예외: -100은 ID 아닌 채점 제외 표시)·패딩 50256(`<|endoftext|>` 칸)·로짓의 칸 번호. 토큰화 = 텍스트를 칸 번호 나열로 바꾸는 것(decode로 역변환).

### instruction-data를 왜 학습하는가 — 지식이 아니라 행동 양식
- 사전훈련만 된 모델 = **자동완성 엔진**: "Fix the grammar: ..."를 주면 지시 수행이 아니라 **비슷한 문제 나열**로 이어씀 (지시를 무시한 게 아니라 "지시받으면 수행" 개념을 배운 적 없음).
- IFT가 가르치는 것: 메커니즘은 같은 다음-토큰-맞히기지만 데이터 패턴이 "`### Instruction` … `### Response:` → 수행 결과" → **"지시 뒤 Response 다음엔 수행 내용이 온다"는 행동 양식** 흡수.
- **1,100건으로 충분한 이유**: 언어력·지식은 사전훈련이 이미 주입 — 가르치는 건 "능력을 어떤 형식으로 꺼내나"뿐. 비유: 대졸자(파운데이션)에게 응대 매뉴얼 단기 연수(지식 교육 아닌 행동 교정). GPT-3(자동완성)↔ChatGPT(비서)를 가른 핵심 재료의 미니 재현.
- ch6과 대비 누적: ch6 = head 교체로 판별 하나를 가르침 / ch7 = head 유지, **데이터 패턴만으로** 범용 지시 수행을 가르침.

### mutex 크래시 재발과 어댑터 자급자족화 (7.4 진입, 2026-07-02)
- 교재 7장 코드를 그대로 치다 `from gpt_download import ...` 부활 → **로컬 TF import 크래시 재발**(`mutex lock failed`). 전역 설정으로 못 막는 import-시점 크래시 — 상세·표준 절차는 [[../../30-References/pytorch-env-hybrid]] 참조.
- 해결하며 `hf_weight_adapter.py`를 **자급자족화**(load_weights_into_gpt 내장, previous_N 의존 제거) → 이후 장에서도 어댑터 한 줄로 사용. 355M(gpt2-medium) 로컬 로드 검증.
- 덤으로 발견: `previous_7.py`가 빈 파일(import 시 지연 폭탄) → 임시로 `previous_6`의 GPTModel 연결. **같은 날 해소**: rasbt 공식 `previous_chapters`(2~6장 모음, TF 없음, generate·train_model_simple·calc_loss 계열 포함)로 채워 `previous_7` import로 복귀, 355M 로드 재검증.

### 배치 처리가 빠른 이유 (GPU 병렬)
- 여러 샘플을 한 텐서로 묶어 넣으면 GPU **코어 수천 개**가 동시 계산 → 1개 처리 시간 ≈ 배치 처리 시간(노는 코어를 채움). 버스 빈 좌석 채우기 비유.
- **코어 ≠ 메모리**(독립 자원, 비례 안 함): 코어=계산 일꾼, 메모리=데이터 창고. 배치 키우면 노는 코어 채워 빨라지지만 **메모리 초과(OOM)** 면 멈춤 → 둘의 균형점에서 batch_size 결정.

## 검증

미세튜닝 전/후 지표 비교 — instruction 응답 품질 개선.

## 관련 노트

- [[llm-from-scratch]] — 마스터 플랜 (인덱스)
- [[llm-ch6-classify]] — 6장 분류 FT (출력·head·손실 대비)
- [[../../30-References/bert-vs-gpt2-classification]] — 트랜스포머·어텐션 개념 정리
- [[../../30-References/instruction-masking-trend]] — 연습문제 7.2 지시 마스킹의 2026-07 연구 동향
- [[../../30-References/pytorch-env-hybrid]] — 환경 정본
