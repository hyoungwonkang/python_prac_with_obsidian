---
title: LLM/ML 개념 용어집
tags: [reference, llm, ml, glossary]
---

# LLM/ML 개념 용어집

> 학습 중 정리하는 **개념·구조 용어** (ch1 체크리스트 1.5에서 예고). 학습 트랙: [[../10-Projects/llm-from-scratch]].
> PyTorch API 용어(tensor·cross_entropy·AdamW 등)는 → [[python-basics]] "PyTorch 기초". MLOps/MLflow는 → [[mlops]].

## AI ⊃ ML ⊃ DL (포함 관계)
```
AI (지능적 작업 전반, 규칙기반 포함)
 └ 머신러닝 ML (데이터에서 패턴/규칙을 스스로 학습)
     └ 딥러닝 DL (여러 층 신경망, 특징도 자동 추출)
         └ 트랜스포머 / LLM (GPT)
```
| 층위 | 정의 | 예 |
|---|---|---|
| AI | 사람 같은 지능적 작업 | 체스 엔진, 전문가 시스템 |
| **ML** | 데이터로 패턴 학습 | sklearn 로지스틱 회귀(Iris) |
| **DL** | 여러 층 신경망 학습 | CNN, 트랜스포머, GPT |

- **고전 ML vs 딥러닝 핵심 차이 = "특징(feature)을 누가 만드나"**:
  - 고전 ML(Iris): 사람이 특징을 정해 줌(꽃받침 길이·너비 4개).
  - 딥러닝(GPT): 특징 추출까지 모델이 자동 → 이게 **백본**의 역할.
- MLflow는 ML **전반**(고전 ML·딥러닝)을 관리 → 그래서 Iris로 배워 GPT에 적용 가능했음.

## 파운데이션 모델 (foundation model)
- **정의**: 방대한 데이터로 사전학습돼 **여러 작업의 토대**가 되는 대형 모델 (예: GPT-2). 2021 Stanford 제안 용어, 언어 외 이미지(CLIP)·멀티모달 포함.
- **교재(Raschka) 틀**: **5장(사전훈련)의 결과물 = 파운데이션 모델.** 그걸 확보하는 활동 = ① 훈련 반복 ② 모델 평가 ③ 사전훈련 가중치 로드. → 6·7장에서 파인튜닝.
  - 즉 "활동 ≠ 파운데이션 모델", "활동으로 파운데이션 모델을 *확보*" (모델은 결과물).
- **실용**: 처음부터 사전훈련은 비쌈(파라미터×토큰×에포크) → **공개 가중치(GPT-2) 로드로 싸게 확보**(5.5).
- 비유: 대학까지 마친 사람(범용 지식, 교육 비쌈) → 파인튜닝 = 특정 직무 단기 연수.

## 백본 (backbone) / head
- **백본**: 입력을 **유용한 표현(특징)으로 바꾸는 핵심 본체**(head 제외 몸통). 등뼈처럼 공통으로 떠받침.
  - GPT 백본 = 임베딩 + 트랜스포머 블록 12개.
- **head**: 백본 출력 위에 붙는 **작업 전용 얇은 층**. GPT 생성 head = `out_head`(768→50257). 분류는 분류 head로 **교체**(6장).
- **파인튜닝** = 백본 재사용 + head 교체(또는 백본 일부만 미세조정).
- **파운데이션 모델 vs 백본**: 파운데이션 = 전체 기반 모델(백본+head), 백본 = 그 안 특징추출 본체.
- 비유: 백본=자동차 섀시/엔진(공통), head=용도별 적재함(트럭·밴·버스).
- 분야: 비전 CNN(ResNet/VGG), 언어(GPT/BERT 트랜스포머 블록) — "특징 추출 본체"의 범용어.

## 모델 = 구조 + 가중치 (2026-07-02 Q&A)
- **구조**: 층이 몇 개고 어떻게 연결되는지 (코드 몇백 줄, 공개·불변). **가중치**: 그 빈칸을 채우는 숫자들(355M = 3.55억 개) — **학습이 바꾸는 건 이것뿐**.
- 비유: 구조 = 믹싱 콘솔 몸체(다이얼 4억 개짜리 판), 가중치 = 다이얼의 현재 눈금. 학습 스텝마다 `새 가중치 = 현재 − 학습률 × 기울기`로 전 다이얼을 조금씩 돌림. 파이썬 변수 `model`은 내내 같은 물건이고 안의 숫자만 교체됨.
- 파인튜닝 lr이 작은(5e-5) 이유: 남이 잘 맞춰놓은 눈금(사전훈련)을 확 돌려 망치지 않으려고.
- **"수렴 = 안정"의 뜻**: 골짜기를 굴러 내려가는 공 — 이동량이 그 지점의 **경사에 비례**하므로 바닥 근처(경사≈0)에선 저절로 감속. **loss 감속 = 가중치가 더 이상 크게 안 움직임 = 한 스텝 더 배워도 모델이 거의 같음**. 데이터 관점: 크고 흔한 패턴부터 흡수(쉬운 점수 소진) → 남은 건 드문 케이스라 티가 안 남.
  - 단 감속 ≠ 좋은 솔루션 보장(어느 웅덩이든 바닥 근처면 감속), train만 내려가고 val이 오르면 수렴 아닌 **과적합**. "train·val 두 곡선이 나란히 눕는 것"이 온전한 수렴.

## .pt 파일도 "모델"이라 부르는가 — 부른다, 단 절반이다
- 실무 관용: "모델 보내줘/다운로드" = 가중치 파일. 구조는 공개 코드(흔함)·가중치는 GPU 수천만 원어치 결과물(귀함) → 정체성이 가중치 쪽에 있어 이름을 가져감.
- 정확히는 **눈금 스냅샷**: 실행하려면 "구조 코드 + 이 파일" 두 조각 필요 → 뼈대 먼저 세우고(`GPTModel(...)`/`from_pretrained`) 채우는(`load_state_dict`) 2단계가 그래서 존재. 뼈대 안 맞으면 shape mismatch.
- .pt 안에 든 것 3종: ① `state_dict`만(표준: `spam_bert.pt`) ② +옵티마이저 상태(학습 재개용: `model_and_optimizer.pth`) ③ MLflow `log_model` = 가중치+구조+환경 통짜(받는 쪽이 뼈대 못 세우는 문제 해소).

## 관련 노트
- [[../10-Projects/llm-from-scratch/llm-ch1-overview]] — 1장 개요(큰 그림)
- [[../10-Projects/llm-from-scratch/llm-ch5-pretrain]] — 파운데이션 모델 확보(사전훈련/가중치 로드)
- [[python-basics]] — PyTorch API 용어 / [[mlops]] — MLOps·MLflow
