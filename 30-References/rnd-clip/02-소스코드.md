# 02 소스코드 (작업본) — CLIP 이미지 상황 판단

> 보고용 자립본: [[export/2_소스코드]]

## 파일 2개

- `export/clip_zeroshot.py` — zero-shot 상황 판정 (PROMPTS 환경변수 = 분류 체계)
- `export/clip_linear_probe.py` — zero-shot vs linear probe 동일 잣대 비교 + 산출물 3종 세트

핵심 설계: 분류 체계를 환경변수 계약으로(프롬프트 교체 = 체계 교체), KoCLIP은 `BASE=` 교체만
(코드 변경 0줄), transformers 5.x `get_image_features` → `.pooler_output` 대응.
상세는 [[export/2_소스코드]].
