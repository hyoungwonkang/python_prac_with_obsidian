# 06 · 전이학습 보고서 — NER 출발점 전략 검증

> R&D 산출물 6호 (2026-07-08, 상사 보고서 양식 8항목 적용). 자립본: [[export/6_전이학습보고서]].
> 실험 상세·해석: [[00-학습메모]] §전이 사슬 A/B 실측. 코드: `export/finetune_wikiann.py`.

## 8항목 요약

| 항목 | 핵심 |
|---|---|
| 수행기간 | 2026-07-05~07 (3일, 로컬 완결) |
| 목표·목적 | 하류 학습(downstream fine-tuning)의 **출발 가중치 전략** 실측 확정 |
| Why·How·What | 분류층은 항상 난수(MISSING) → 출발점 2안 존재 / 같은 조건 A/B 대조 + 부분 로드 / 기본값 도출 |
| 수행 과정 | 흐름도 + 라벨 ENUM(7태그) + 로드 상태코드(LOADED·UNEXPECTED·MISSING) 표 — 양식의 "상태코드" 요구를 ML 대응물로 충족 |
| 수행 결과 | A(원본) **F1 0.8447** vs B(사슬) 0.8317 — **사슬 추가 이득 미확인** (시드 1회 단서) |
| 기대 효과 | 도메인 NER 기본 전략 확정(원본 출발)·양 라벨 체계 파이프라인·비용 0 |
| 산출물 | finetune_wikiann.py, 가중치 3종, 문서 6호까지 |
| 향후계획 | 업무 도메인 전이(본 목적) → 소량 데이터 재검증 → ko-pii 하이브리드 |

용어 표기: 분류층(classification head)·하류 학습(downstream fine-tuning)·어절 단위(word-level) 병기.
