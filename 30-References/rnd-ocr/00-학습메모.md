# 00 · 학습메모 — OCR R&D에서 익힌 개념 (2026-07-14~)

> 지시 3.1(Paddle·EasyOCR) 진행 중 Q&A로 체득한 **새 개념만** 누적 (보고물 아님).
> 선행 개념은 반복하지 않음: 평가지표(P·R·F1)는 [[../ml-eval-training-terms]],
> softmax·억지 배정·확신형은 [[../rnd-clip/00-학습메모]] 11~14,
> 전처리·비식별(OpenCV)은 [[../rnd-detection-models-2/00-학습메모]].

## OCR의 구조

### 1. OCR = 검출 + 인식, 2단 파이프라인
- 출력이 3요소인 이유: `(박스 좌표, 텍스트, 신뢰도)` — **검출(글자가 어디에) + 인식(그 글자가
  무엇인지)**이 한 몸이라 위치와 텍스트가 함께 나온다. YOLO(어디)+분류(무엇)의 결합판.
- EasyOCR 내부: 검출=CRAFT, 인식=CRNN (둘 다 PyTorch 사전학습 — 우리 쪽 **학습 0줄, 추론만**).
  성격은 PII(ko-pii)·OpenCV와 같은 라이브러리 실습, 아키텍처 ②추출 계층.

### 2. 깨끗한 이미지도 오독한다 — OCR은 이해가 아니라 모양 추정
- 첫 실측(clean, 완벽 렌더링): CER 9.5%, 4글자 오독 — **전부 모양이 비슷한 글자끼리**
  (첨→점, 을→올, 1→/, 랍→람). 획 하나 차이는 구조적 약점.
- 신뢰도가 자기 오독을 표시: 오독 2곳 줄이 0.325로 스스로 낮게 채점 → 검수 라우팅의 근거.
- 하류 파이프라인 조짐: 스팸 어휘("무료 상품권")는 생존 → 분류는 견딜 가능성 /
  전화번호가 `0/0-…`로 깨짐 → **PII 정규식은 놓칠 수 있음** — OCR 오류가 하류 탐지를
  무너뜨리는 경로의 실물.

## 채점 잣대

### 3. CER = 편집거리 ÷ 정답 글자 수 — 부분 점수의 잣대
- 받아쓰기 채점: "정답 글자 100개당 몇 개 틀렸나". 0%=완벽, 낮을수록 좋음(F1과 방향 반대),
  100% 초과 가능. 한국어는 교착어라 단어 단위(WER)가 아닌 **글자 단위(CER)가 표준**.
- 지표 지도에서의 자리: accuracy·F1(건 단위 O/X)·mAP(박스)와 달리 CER은 문자열이
  **얼마나** 틀렸는지의 부분 점수 — "거의 맞음(9.5%)"과 "붕괴(64.3%)"를 한 숫자로 가름.

### 4. 편집거리 = 가장 짧은 고치기 경로 — 밀림은 길이 변화형만
- 최소 수정(치환·빠짐·끼어듦) 횟수. **"최소"가 핵심** — 같은 두 문자열도 서툰 길(3회)과
  영리한 길(1회)이 있고, 편집거리는 항상 후자. DP 표 채우기로 계산(ocr_eval.py:22-33), 대칭.
- 자리별 비교(눈 대조)가 무너지는 원인은 삭제가 아니라 **길이 변화**(빠짐·끼어듦) — 뒤 전체가
  밀림. 치환은 길이 불변이라 안 밀림. "빠짐=삭제냐 삽입이냐"는 변환 방향이 붙이는 이름표일 뿐.
- 눈 대조의 한계 실증: 같은 오류를 나는 "4곳", 코드는 "3구간(4글자)"로 셈 — 사람은 세는
  기준이 흔들린다. clean에서 눈 대조가 통한 건 오류가 마침 치환뿐이었던 우연.

### 5. 인스펙터(inspector)와 잣대의 분리 — 지시 2 패턴 재사용
- `ocr_inspect.py`(인스펙터) = 정답 모름, 신뢰도·박스까지 표시 → 오답 **원인** 분석용 /
  `ocr_eval.py`(잣대) = 정답 대조 CER, `make_reader()`로 **엔진 중립** → 점수는 여기서만.
- rule_spam.py ↔ eval_compare.py 구조 그대로 — 잣대가 특정 엔진 안에 있으면 "각자 자기
  자로 잰" 꼴. easy|paddle을 같은 자로 재기 위한 분리.

## 실험 설계

### 6. 열화 4종 = 의도적 파괴 — 전처리 가치 증명의 대조군
- 열화는 실무 저품질의 표본이자 **OpenCV 전처리 4종의 시험지**: 회전→기울기 보정 /
  노이즈→디노이즈 / 저대비→이진화 / 축소→확대. 망가진 기준선 없이는 "전처리가 살렸다"를
  증명할 수 없다 (CLIP 쇼핑몰 대조 실험과 같은 구조).
- 기준선 실측(EasyOCR·CPU): clean 9.5 / lowcontrast 7.1 / noisy 7.1 / **rotated 64.3** / small 9.5(%).

### 7. rotated 64.3%의 실체 = 어순 붕괴 — 그리고 비단조 관찰
- 글자를 못 읽은 게 아니라 **검출 박스의 줄 묶음·정렬이 꼬여 순서가 붕괴** → 빠짐·끼어듦
  대량 → 편집거리 폭발. 기울기 보정이 정확히 이걸 고치는 전처리 (다음 실험 1순위).
- 예상 밖: lowcontrast·noisy가 clean보다 좋음(7.1 < 9.5) — 열화로 `1→/` 오독이 사라짐.
  **화질과 인식은 단조 관계가 아니다.** 단 문장 1개(n=1) 결과 — 일반화 금물.

## 운용 원칙

### 8. 장치 보수주의 — 이득 0이면 위험도 사지 마라
- OCR 채점은 CPU 기본(GPU=1 옵션만). 근거: ① MPS 조용한 오답 전례
  ([[../../10-Projects/llm-from-scratch/llm-ch7-failure-log]] — 비연속 가중치+MPS 행렬곱,
  같은 배치 CPU 3.81 vs MPS 19.77) ② 추론 0.1~0.2초/장이라 GPU 이득이 사실상 0
  ③ ocr_eval은 **잣대** — 장치 따라 흔들리면 모든 비교가 오염. 장치 고정 = seed 고정과
  같은 변수 통제. (학습처럼 속도가 아쉬울 땐 MPS + 수치 검증으로 상쇄 — 국면별 계산.)

### 9. 신뢰도 = 인식 모델의 softmax 확률 — 표준 출력, 같은 족보
- readtext 3요소의 신뢰도는 EasyOCR 전용 기능이 아니라 확률 기반 엔진의 표준 부산물
  (PaddleOCR은 rec_scores). BERT %·YOLO 0.92·CLIP %와 같은 "모델의 자기 확신".
- 자기 확신 ≠ 정답 보증: 모양이 아주 비슷한 오독은 **높은 신뢰도로 당당히 틀릴 수 있다**
  (OCR판 확신형 — [[../rnd-clip/00-학습메모]] 억지 배정 유형론의 재현).

### 10. CER ↔ 신뢰도 = 국면 릴레이 — 그리고 검수는 사람이다
- **평가 국면**(정답 있음): CER 주역, 신뢰도는 참고 / **운영 국면**(정답 없음 — 알았다면
  OCR이 불필요): CER 계산 불가, **신뢰도가 유일한 품질 신호**. R&D의 숙제 = CER로 신뢰도
  문턱을 검증해(validation 정신) 운영 라우팅에 인계.
- 검수 = 사람의 직접 확인·수정. 신뢰도는 **사람 시간의 배분기**(확신 높음→자동 통과,
  낮음→검수 큐) — 50% 경고·의사 라벨링과 동일 패턴이고, 고쳐진 쌍은 골드 데이터로 축적돼
  재학습 순환에 편입. 사람 앞에 자동 재시도 사다리(전처리 재OCR→타 엔진→사람) 가능.
- 이 도메인 특수성: 검수량 축소가 곧 **검수자 보호**(유해물 노출 최소화), 검수 화면은 비식별본.

## 전처리 실험 (2026-07-14~15 Q&A 누적)

### 11. 전처리 = 본 처리 전 "조건 복구" — 천장은 엔진 실력
- 전처리(pre+processing) = 주인공 모델 앞에서 입력을 손질하는 모든 작업. 조건(기울기·잡음·
  대비·해상도) 관점으로는 **깨진 조건을 되돌리는 일** — 학습 0, 엔진을 똑똑하게 만드는 게
  아니라 **엔진이 제 실력을 내게** 함. 그래서 천장 = clean 점수(엔진 본질 한계).
- deskew의 요령: 글자를 못 읽어도 **글자 픽셀 무리의 기울기는 기하로 잴 수 있다**
  (Otsu→findNonZero→minAreaRect — 8° 이미지에서 -7.996° 검출). 내용 이해 없는 진단.

### 12. CER 분해 — "엔진 탓"과 "열화 탓"이 갈라진다
- deskew 복구 후 오독이 clean과 **글자까지 동일** → rotated 64.3% = 엔진 탓 9.5% + 열화 탓
  54.8%p로 분해됨을 실증. 전처리는 열화 탓만 지울 수 있고, 엔진 탓은 엔진 교체로만 도전.
- 기준선의 좌표 역할: clean = 전처리의 천장 / rotated = 개선 여지의 크기. 미리 재둔 덕에
  "완전 복구"를 단언 가능. **기준선 = 개선 조치 전의 출발점 성적** — 없으면 "좋아졌다" 불가.

### 13. 표적 치료 — 병 없으면 무익, 잘못된 약은 유해, 약효는 환자 의존
- 병(회전) 있으면 극적(−54.8%p) / 병 없으면(저대비·노이즈 — 애초 잘 읽힘) 무익 /
  진단 없는 약(확대)은 유해 — 보간 얼룩을 유령 글자로 읽음(Easy 9.5→11.9%).
- 같은 upscale이 Paddle엔 무해 — **전처리 효과는 엔진 의존**. before/after 채점 후 적용이 수칙.

### 14. 조건 통제와 코드 렌더링 — 변인 통제 실험
- 성적 = 이미지 조건 × 엔진 실력. 조건을 만점 고정(코드 렌더링)하면 남는 변수는 실력뿐 →
  clean 오류는 엔진 몫. 열화 4종 = 조건표에서 하나씩 골라 파괴한 것 (탓의 분리).
- 사진·웹 대신 렌더링한 이유 = 조건 완전 통제 + 정답 보유(CER 가능). 두 기둥 모두 여기서.

### 15. n=1 요동 — 신호와 우연을 가르는 법
- 42자 실험에서 글자 1개 = 2.4%p — 경계선 글자(1↔/ 박빙) 하나가 우연히 넘어지면 그만큼
  출렁임. lowcontrast 7.1% < clean 9.5%가 그것 — "저대비가 이롭다" 결론 금물.
- 구분 기준 3가지: **크기**(요동 단위 대비)·**메커니즘**(설명 가능한 원인)·**재현성**.
  rotated 54.8%p(요동의 23배·어순 붕괴 메커니즘)는 신호, 2.4%p는 요동. 처방 = n 확대.

## PaddleOCR 비교·파이프라인 (2026-07-16 누적)

### 16. 제2 프레임워크의 격리 — venv 분리가 실제로 막은 것
- PaddleOCR은 PyTorch가 아니라 **PaddlePaddle(별개 딥러닝 프레임워크)** 위에 있음 — 설치가
  "제2의 엔진 통째로"라 의존성·플랫폼(mac arm64 변방)·동거(OpenMP) 3중 위험.
- 격리 실증: Paddle 동반 opencv 4.10·numpy 2.3.5 ≠ rnd-env 5.0/2.5.1 — 같은 패키지의 다른
  버전이 각자 방에서 공존. 관문 자체는 순조(악명은 실측으로 반증) — "실측이 답"의 재확인.
- 검증 3방향: 작동(run_check)·무누출(rnd-env에 paddle 없음)·본 환경 무손상(고정 버전 전부 불변).

### 17. 비교 결과 읽기 — 단일 승자 없음의 재현
- Paddle 5전 5승(clean 4.8%) — 개별 격차는 요동 언저리나 **5장 전부 같은 방향** = 우연으로
  보기 어려운 경향 (n=1 규율과 일관 판단의 공존).
- **오답 프로필 상이**: Easy=모양 혼동 치환(첨→점·1→/·랍→람) / Paddle=문장 끝 글자 절단
  (다→ㄷ/∅). 서로의 오독을 서로 맞힘 → 교차 검증(불일치만 검수) 앙상블 여지 — 지시 2 재현.
- 회전은 엔진 교체로 미해결(47.6%) — 내장 doc_ori·UVDoc은 90°급 회전용, 8° 미세 기울기는
  못 잡음. **deskew는 엔진 무관 유효**, deskew+Paddle=2.4% 전체 최고. 속도는 Easy 5~10배.

### 18. ②품질=③성능 — 하류·연쇄 붕괴·계층별 게이트
- **하류(downstream)** = 흐름의 아래쪽 (강 비유): 오염은 아래로만 전파, 하류는 상류가 준 것
  이상을 할 수 없다. 파이프라인 하류(②→③)와 전이학습 하류 과제(사전학습→파인튜닝)가
  같은 법칙 — 문제는 상류에서 고칠수록 싸다.
- 실증: 한 글자(`1→/`)가 전화번호 패턴을 깨자 PII **전멸**(4건→0건, MEDIUM→INFO) — 온전한
  이름까지 연쇄 미탐(문맥 결합 추정). 오독 조각은 NER 유령 개체(0[QT])도 생성 — 오탐도 전파.
- 과제별 내성: **중복 단서 과제**(스팸·NER — 단서 하나 깨져도 받침)는 견디고,
  **형식 과제**(PII — 패턴이 생명, 여분 0)는 한 글자에 무너짐.
- **뒤 계층의 신뢰도는 앞 계층의 오염에 침묵한다** — 오염 텍스트에 PII는 "당당히 0건"(조용한
  실패, 검수 대상으로도 안 뜸). 처방 = 계층마다 자기 신뢰도로 자기 문 앞을(OCR 0.627 게이트가
  유일한 방어선이었음). 최종 신뢰성 = 계층 신뢰도의 사슬, 약한 고리가 전체를 정함.

## 앙상블·실사 확장·운용 설계 (2026-07-16 누적)

### 19. 기술이 사는 3층 — 호출/구현/지식 (CRAFT·CRNN의 실제 위치)
- 우리 코드는 **호출 2줄**(Reader·readtext) = 인터페이스. 기술 구현은 라이브러리 내부
  (site-packages/easyocr: craft.py·detection.py·recognition.py), 배운 지식은 가중치
  (~/.EasyOCR/model: 검출 83MB + 한국어 인식 16MB — 언어 목록이 인식 가중치를 고름).
- 2단 파이프라인이 코드로도 두 줄: easyocr.py의 `self.detect()`(456행)→`self.recognize()`(468행).
  출력 3요소의 출처가 이 릴레이 (박스=검출, 텍스트·신뢰도=인식).
- 삼형제 출력 차이 = 배운 정답의 모양: YOLO(박스 라벨로 배움→박스), CLIP(캡션으로 배움→어울림
  %만 — 박스·텍스트 없음), OCR(글자 박스+글자열로 배움→둘 다). [[../rnd-clip/00-학습메모]] 13의 확장.

### 20. 앙상블 = 오류 비상관의 활용 — 4지표 읽는 법
- 전제: **틀리는 자리가 서로 달라야** 효과 (구조·학습 데이터가 다른 두 엔진). 지시 2
  HYBRID-AND(오탐 교집합 0)의 OCR판 — "실패 방식이 다른 둘을 겹치면 겹치는 실패만 남는다".
- 4지표: 합의(자동 통과량) / 검수 부담(**비용**) / 맹점(둘이 같은 오답에 합의 — **위험**,
  조용한 실패) / 정책 결과(교정 가정 최종 정확도). **정책 결과 = 100%−맹점의 동어반복** —
  지표 설계를 알면 과대 해석을 피함. 정보량 있는 숫자는 맹점과 부담 둘.
- 합성 실측: 맹점 0(10회 채점 전부) — 단 **경향이지 보장 아님**(표본 소). 부담 50→42%(전처리
  결합) — 최악 조건 수치.
- 실행 구조: 교대가 아니라 **엔진당 전용 환경 서브프로세스 1회 일괄**(모델 로드 1회), 결과는
  파일로 합류, 대조는 엔진 없는 순수 파이썬 — 물리(격리)와 원리(OpenMP)의 이중 필연.

### 21. 실사의 새 병리 = 유령 텍스트(환각) — 그리고 앙상블의 존재 가치 실증
- 합성의 병리가 오독(치환)이라면 실사는 **무늬·질감을 글자로 읽는 환각**: `'{'`(신뢰도
  0.89!)·`bqklqvq`·한글 환각(`화 비ilili`) — **확신형 오탐의 실물**, 신뢰도 게이트(0.7)를 통과.
- coco128 실측: 그 0.89 유령을 **앙상블 불일치가 포획**(easy `7;{` vs paddle `RUCANOR.`) —
  "타 엔진의 증언"은 자기 신고(신뢰도)와 독립 원리라 확신형을 잡는다. 제3 방어선 가설 실증.
- 합의는 진짜 텍스트에 몰림(PEACE WAY HOTEL — 쓰레기는 서로 다르게 틀려 불일치). 장면
  텍스트에선 승패 갈림(AIRFRANCE=easy승, NOKIA·STOP=paddle승) — 단일 우세 없음의 실사 재확인.

### 22. 운영 모드의 도구화 — 그리고 도메인 불일치
- 정답 없는 데이터(coco)에선 CER 자동 불가 → 같은 도구가 정답 파일 유무로 **평가/운영 모드
  자동 분기**(ocr_ensemble.py) — 국면 릴레이를 코드 한 파일에 구현. 운영 모드에선 맹점·정확도
  측정 불가(합의/불일치만) — "안전성 검증은 평가 국면의 몫"이 실물로.
- 냉정한 실측: 장면 텍스트 단어 합의 8% → 텍스트 문서 전량 검수행 — **검수량 축소 효과는
  도메인 의존**. 실익은 무텍스트 필터(64% 자동 스킵)+검수 품질(두 후보 제시·오탐 노출).
- **도메인 불일치** 개념: coco=거리 간판(장면 텍스트) ≠ 우리 도메인=채팅 캡처(문서형).
  합성 clean(합의 50%)이 오히려 실무에 가까운 조건 — 결론은 도메인 파일럿이 정함.
  개선 여지: 신뢰도 하한 필터 후 대조(쓰레기 불일치 축소).

### 23. 용량 산정 — 기계는 병목이 아니다, 병목은 사람
- 1만 장 외삽(실측 계수): 순차 CPU ~5시간(앙상블) / 워커 10개 ~30분 / GPU 분 단위 —
  이미지가 독립이라 **병렬화는 나눗셈**(아키텍처 §3 Worker Pod 구조가 이것). 전처리는 무시 가능.
- **진짜 비용 = 검수 큐의 사람 시간**(추정 3~7시간) → 최적화 과녁은 속도가 아니라 검수량
  축소(전처리로 불일치↓·위험도 우선순위·신뢰도 문턱). 검수량 축소 = 비용 절감 + 검수자 보호.
- 산정 절차: 도메인 이미지 파일럿으로 3계수(장당 시간·텍스트 포함률·불일치율) 실측 후 외삽 —
  장당 시간은 글자량 비례, 텍스트 포함률은 도메인 따라 16%~100%.

### 24. 검수의 자리 — 우회로가 아니라 수리소
- 신뢰도 낮아 검수된 문서도 **교정 후 반드시 ③으로 합류** — 사람이 고치는 건 ②의 일(글자
  복원)이지 ③의 일(판정)이 아님. 증거 = 파이프라인 "정답 행"(교정 텍스트 → PII 4건 정상 탐지).
- 검수 지점은 2곳: ② 뒤 텍스트 검수(글자가 맞나 → ③으로) / ③·⑦ 뒤 판정 검수(Human Review —
  판정이 맞나 → 사건화). 같은 원리(낮은 확신→사람), 다른 대상.
- **검수 큐** = 사람 검수 대기열: 기계(초당 수십 건)와 사람(분당 몇 건)의 속도 차 완충 장치.
  넣을지는 신뢰도/불일치가, 줄 순서는 위험 등급(아키텍처 §6: Critical 즉시 대응~Low 모니터링)이
  정함. 데모 검수 탭이 최소판.

## 관련 노트

- [[../../10-Projects/detection-ai-study]] — 상위 로드맵 (지시 3.1, 마지막 남은 항목)
- [[../rnd-detection-models-2/00-학습메모]] — OpenCV 전처리·비식별 (다음 실험의 도구)
- [[../rnd-clip/00-학습메모]] — softmax·억지 배정·확신형 (신뢰도 이해의 뿌리)
- [[../rnd-rule-vs-bert/00-학습메모]] — 동일 잣대 비교·문턱 조정 원칙의 출처
