# 소스코드 — BERT 스팸 분류

첨부 파일: `dataset_finetuning.py` (데이터·라벨링), `finetune_bert_spam.py` (학습·평가).
아래는 전체 코드와 R&D 3주제 매핑.

## 코드 구조 ↔ R&D 3주제

| 코드 위치 | R&D 주제 |
|---|---|
| `dataset_finetuning.py` 전체 | 라벨링 방법론 (인코딩·균형화·분할) |
| `finetune_bert_spam.py` ① 데이터 로드 | 라벨링 결과 사용 |
| ② 토크나이즈 + 패딩 | BERT 입력 준비 |
| ③ `BertForSequenceClassification` | BERT 모델 |
| ④ 학습 루프 | BERT 파인튜닝 |
| ⑤ 테스트셋 평가 | 테스트 |

---

## 1) 데이터·라벨링 — `dataset_finetuning.py`

```python
import urllib.request
import zipfile
import os
from pathlib import Path

url = "https://archive.ics.uci.edu/static/public/228/sms+spam+collection.zip"
zip_path = "sms_spam_collection.zip"
extracted_path = "sms_spam_collection"
data_file_path = Path(extracted_path) / "SMSSpamCollection.tsv"

def download_and_unzip_spam_data(url, zip_path, extracted_path, data_file_path):
    if data_file_path.exists():
        print(f"{data_file_path}가 이미 있어 다운로드 및 압축 해제를 건너뜁니다.")
        return
    with urllib.request.urlopen(url) as response:
        with open(zip_path, "wb") as out_file:
            out_file.write(response.read())
    with zipfile.ZipFile(zip_path, "r") as zip_ref:
        zip_ref.extractall(extracted_path)
    original_file_path = Path(extracted_path) / "SMSSpamCollection"
    os.rename(original_file_path, data_file_path)
    print(f"파일이 다운로드되어 {data_file_path}에 저장되었습니다.")

download_and_unzip_spam_data(url, zip_path, extracted_path, data_file_path)

import pandas as pd
df = pd.read_csv(data_file_path, sep="\t", header=None, names=["Label", "Text"])
print(df["Label"].value_counts())

# 클래스 균형화 — 다수 클래스(ham)를 소수 클래스(spam) 개수로 언더샘플링
def create_balanced_dataset(df):
    num_spam = df[df["Label"] == "spam"].shape[0]
    ham_subset = df[df["Label"] == "ham"].sample(num_spam, random_state=123)
    balanced_df = pd.concat([ham_subset, df[df["Label"] == "spam"]])
    return balanced_df

balanced_df = create_balanced_dataset(df)
print(balanced_df["Label"].value_counts())

# 라벨 인코딩 — ham→0, spam→1
balanced_df["Label"] = balanced_df["Label"].map({"ham": 0, "spam": 1})

# 분할 — train 70% / validation 10% / test 20%
def random_split(df, train_frac, validation_frac):
    df = df.sample(frac=1, random_state=123).reset_index(drop=True)
    train_end = int(len(df) * train_frac)
    validation_end = train_end + int(len(df) * validation_frac)
    train_df = df[:train_end]
    validation_df = df[train_end:validation_end]
    test_df = df[validation_end:]
    return train_df, validation_df, test_df

train_df, validation_df, test_df = random_split(balanced_df, 0.7, 0.1)
train_df.to_csv("train.csv", index=None)
validation_df.to_csv("validation.csv", index=None)
test_df.to_csv("test.csv", index=None)
```

---

## 2) 학습·평가 — `finetune_bert_spam.py`

```python
import os
os.environ["USE_TF"] = "0"        # HF가 TensorFlow/Flax를 임포트하지 않도록 차단
os.environ["USE_FLAX"] = "0"

from pathlib import Path
import pandas as pd
import torch
from torch.utils.data import Dataset, DataLoader
from transformers import BertTokenizer, BertForSequenceClassification
from torch.optim import AdamW
from sklearn.metrics import accuracy_score, confusion_matrix, classification_report

MODEL_NAME = "bert-base-uncased"
MAX_LENGTH = 128
BATCH_SIZE = 16
EPOCHS = int(os.environ.get("EPOCHS", 3))
LR = 2e-5
SEED = 123

DATA_DIR = Path(".")   # train/validation/test.csv 위치

torch.manual_seed(SEED)
device = "mps" if torch.backends.mps.is_available() else (
    "cuda" if torch.cuda.is_available() else "cpu")
print(f"device: {device} | epochs: {EPOCHS}")

# ② Dataset: SMS 1건을 토크나이즈 + 패딩
class SpamDataset(Dataset):
    def __init__(self, csv_path, tokenizer):
        self.df = pd.read_csv(csv_path)
        self.tokenizer = tokenizer
    def __len__(self):
        return len(self.df)
    def __getitem__(self, idx):
        row = self.df.iloc[idx]
        enc = self.tokenizer(str(row["Text"]), padding="max_length",
                             truncation=True, max_length=MAX_LENGTH, return_tensors="pt")
        return {"input_ids": enc["input_ids"].squeeze(0),
                "attention_mask": enc["attention_mask"].squeeze(0),
                "label": torch.tensor(int(row["Label"]), dtype=torch.long)}

def evaluate(model, loader):
    model.eval()
    preds, golds = [], []
    with torch.no_grad():
        for batch in loader:
            logits = model(input_ids=batch["input_ids"].to(device),
                           attention_mask=batch["attention_mask"].to(device)).logits
            preds.extend(logits.argmax(dim=-1).cpu().tolist())
            golds.extend(batch["label"].tolist())
    return golds, preds

def main():
    # ① 데이터·라벨 로드
    tokenizer = BertTokenizer.from_pretrained(MODEL_NAME)
    train_ds = SpamDataset(DATA_DIR / "train.csv", tokenizer)
    val_ds   = SpamDataset(DATA_DIR / "validation.csv", tokenizer)
    test_ds  = SpamDataset(DATA_DIR / "test.csv", tokenizer)
    train_loader = DataLoader(train_ds, batch_size=BATCH_SIZE, shuffle=True)
    val_loader   = DataLoader(val_ds, batch_size=BATCH_SIZE)
    test_loader  = DataLoader(test_ds, batch_size=BATCH_SIZE)

    # ③ 모델 — [CLS] + 분류층 (클래스 2개)
    model = BertForSequenceClassification.from_pretrained(MODEL_NAME, num_labels=2).to(device)
    optimizer = AdamW(model.parameters(), lr=LR)

    # ④ 학습 루프
    for epoch in range(1, EPOCHS + 1):
        model.train()
        total = 0.0
        for batch in train_loader:
            optimizer.zero_grad()
            out = model(input_ids=batch["input_ids"].to(device),
                        attention_mask=batch["attention_mask"].to(device),
                        labels=batch["label"].to(device))
            out.loss.backward()
            optimizer.step()
            total += out.loss.item()
        val_gold, val_pred = evaluate(model, val_loader)
        print(f"[epoch {epoch}] train_loss {total/len(train_loader):.4f} "
              f"| val_acc {accuracy_score(val_gold, val_pred):.4f}")

    # ⑤ 테스트셋 최종 평가 (학습 미사용)
    gold, pred = evaluate(model, test_loader)
    print("\n=== TEST 결과 (test.csv 300건) ===")
    print(f"accuracy: {accuracy_score(gold, pred):.4f}")
    print("confusion matrix [행=실제, 열=예측] (0=ham,1=spam):")
    print(confusion_matrix(gold, pred))
    print(classification_report(gold, pred, target_names=["ham(0)", "spam(1)"], digits=4))

if __name__ == "__main__":
    main()
```

> 첨부된 `.py` 파일은 데이터 경로를 자동 탐색하도록 작성돼 있으며, 위 코드는 동일 폴더에 CSV가 있을 때를 가정한 정리본이다.

## 의존성
`torch >= 2.8` · `transformers 4.57.x` · `scikit-learn 1.6.x` · `pandas` (TensorFlow 불필요)

## 검증된 출력 (3 epoch, Apple Silicon MPS)
```
[epoch 1] train_loss 0.2286 | val_acc 0.9933
[epoch 2] train_loss 0.0493 | val_acc 0.9933
[epoch 3] train_loss 0.0351 | val_acc 0.9933

=== TEST 결과 (test.csv 300건) ===
accuracy: 0.9767
confusion matrix [[148 1] / [6 145]]
spam  precision 0.9932  recall 0.9603  f1 0.9764
```
