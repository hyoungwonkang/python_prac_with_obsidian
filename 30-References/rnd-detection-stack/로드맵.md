# 로드맵 — rnd-detection-stack (OCR 포함 통합 탐지·분석 계층)

> **목적**: OCR까지 포함한 전 모듈이 "받는 사람 관점"으로 실행되는 발송용 통합 패키지의 정본 재구축.
> 기존 조립기(`rnd-report-package`)와 이전 발송 스냅샷(`탐지계층-R&D-보고`)은 **발송 내용 불명**으로
> 2026-07-19 `99-Archive/` 레거시 보관 — 이 폴더가 앞으로의 단일 기준.

## 배경 (왜 새로 만드는가)

- 상사에게 보낸 정확한 내용을 알 수 없음 → 조립기와 스냅샷이 산재해 혼란 가중 → 레거시 동결 후 재출발.
- 전제 확정: **상사는 보고 때마다 새 파일을 받는다** — 이전 파일·가중치·데이터 보유 가정 금지.
- 배포 형태 확정: **경량본** — .pt(스팸·NER·YOLO 가중치) 미포함, 받는 쪽이 동봉 데이터·자동 다운로드로 재생성.
- 레거시 검증(2026-07-19)에서 결함 3건 실측 확인 — Step 2에서 수정.

## 확보된 재료 (2026-07-19 실측)

- 모듈 6종 export 전부 존재 (`30-References/`): dataset-artifacts(17)·rule-vs-bert(8)·clip(7)·uxui-demo(3)·detection-models(19)·ocr(10)
- 스팸 재생성용 csv 생성 완료: `rnd-bert-labeling-test/export/ko/` — train 1045·val 149·test 300 (HF 공개셋, 스크립트 재생성 가능)
- `~/rnd-env` 검증 완료: **Python 3.12.13**(uv 독립 빌드) + requirements 고정 조합 전체 임포트 OK, CUDA 가용(WSL)
- 한국어 폰트(WSL): `/mnt/c/Windows/Fonts/malgun.ttf` — `FONT=` 환경변수로 지정

## Step 0 — 레거시 보관 ✅ (2026-07-19)

- `99-Archive/rnd-report-package/` — 조립기 build.py + 통합 문서 (참고용으로 열람 가능, 수정 금지)
- `99-Archive/탐지계층-R&D-보고/` — 이전 발송 추정 스냅샷 (중복 2본 → diff 동일 확인 후 1본 통합)

## Step 1 — 환경 완성

- [x] `~/rnd-env` — Python 3.12 + 고정 requirements (2026-07-19)
- [x] `~/ocr-env` — paddlepaddle 3.3.1 + paddleocr 3.7.0 격리 venv (2026-07-19, 리눅스 wheel 정상)

## Step 2 — 알려진 결함 수정 (레거시에서 실측된 3건)

- [x] `ocr_pipeline.py`·`ocr_ensemble.py`: `~/rnd-env/bin/python` **하드코딩 제거** (2026-07-19) — easy=`sys.executable`(현재 파이썬 재사용), paddle=OS 분기(`bin`/`Scripts`) + `OCR_ENV=` 오버라이드.
- [x] requirements.txt "최소 3.10" 표기 → **"Python 3.12 필수"** (2026-07-19, 이 폴더 requirements.txt로 신규 작성 — numpy 2.5.1이 ≥3.12)
- [x] rnd-ocr `3_사용법.md`(개별 export)에 ocr_ensemble(⑦) 절 보완 + 작업본 빠른 실행에 ⑦ 추가 (2026-07-19)
- [x] **(추가 발견·수정)** `ocr_eval.py` paddle 리더에 `enable_mkldnn=False` — paddle 3.3.1 oneDNN 커널이 일부 CPU에서 NotImplementedError(WSL 실측). 채점은 CPU 고정이라 속도 영향 무시.
- 수정은 원본(`30-References/rnd-*`)에서 하고 이 폴더로 조립 (원본=정본 원칙 유지)

## Step 3 — 가중치 재생성 리허설 (경량본 수신자 절차 그대로)

- [x] 스팸: `train_text.py` (동봉 csv) → 3종 세트 (2026-07-19 WSL/CUDA — 테스트 정확도 0.9567, 20초)
- [x] NER: `finetune_ner.py` (KLUE 자동 다운로드) → ner_klue.pt (2026-07-19 — micro F1 0.71)

## Step 4 — 전 모듈 스모크 (받는 사람 관점, 사용법 문서 순서대로)

- [x] OCR ①~⑦ (2026-07-19 WSL 전부 통과) — easy clean 7.1%·rotated 61.9%→deskew 9.5% / paddle clean 2.4% / 파이프라인 3소스 전화 PII ✅ / 앙상블 전처리 결합 **맹점 0·검수 32%** (수정된 경로 로직으로 실증)
- [x] 통합 데모 `app.py` 기동 스모크 (2026-07-19 — 재생성 가중치로 HTTP 200. 탭별 상호작용은 Step 6 리허설에서 육안 확인)
- [x] rule-vs-bert(문턱 0.9463·BERT 0.9567)·clip(문서 텍스트 98.4%) 스크립트 (2026-07-19)

## Step 5 — 조립기 재정비 + 경량본 조립

- [x] build.py 이관·갱신 (2026-07-19) — csv 누락 시 **중단**(조용한 건너뜀 제거), 경로 세정을 타 PC 경로까지 확장(맥 `/Users/…` 잔존 실측→정규식 세정), **경량본(WEIGHTS=0) 기본**
- [x] **README 생성 규칙**: `README_mac.md` / `README_windows.md` 분리 유지 — 레거시에서 이관 + "Python 3.12 필수" 반영, numpy 증상 행 추가 (2026-07-19)
- [x] 조립 산출물 **날짜 포함 명명** — 기본 `~/Desktop/탐지분석계층-보고-<날짜>` (2026-07-19 조립 실증: 코드 31개+csv 3개, .pt 0개, 520KB)
- [x] PDF 2종(5_도식도·6_보고서) 생성 (2026-07-19) — make_pdf.py를 OS 무관하게 수정(Chrome 자동 탐지: 맥 앱>PATH>puppeteer 캐시, `CHROME=` 오버라이드). WSL 전제: 한글 폰트를 `~/.fonts`에 설치(맑은고딕 복사)해야 도식 한글이 안 깨짐 — 미설치 시 전부 □로 렌더(실측). 재조립·zip 갱신 완료(672KB)

## Step 5′ — 추가 요구 반영 (2026-07-19 오후, 사용자 지시)

- [x] **rnd-detection-models-2(OpenCV 실습— 비식별화 블러 포함) 패키지 편입** — build.py FOLDERS 추가, README 산출물 표 7행·폴더 지도·3_사용법 8) 신설. deidentify 스모크 통과(사람 2명 검출→블러).
- [x] **데모에 OCR 추출(②) 탭 신설** — OpenCV deskew 자동 적용(전처리 전후 이미지·추출 결과 나란히) + **EasyOCR·PaddleOCR 교차 검증**(단어 합의/불일치 표시, 합의=자동·불일치=검수 정책 문구). Paddle은 격리 venv 서브프로세스(OCR_ENV 오버라이드), 없으면 EasyOCR만+안내. 기능 테스트: 회전 이미지에서 Easy 확신 86% 오독(`070`)을 Paddle(`010`) 불일치가 포획 — 확신형 오탐 방어가 화면에서 재현됨. 데모 기동 스모크 HTTP 200 + OCR 탭 존재 확인.

## Step 6 — 조립본 최종 리허설 → 발송

- [x] 조립본 복사본을 별도 위치(/tmp)에서 **README만으로 처음부터 실행** (2026-07-19) — 패키지 안 새 venv(3.12) 설치→임포트 검증→스팸 0.9633·NER F1 0.71 재생성→OCR ①~⑦(앙상블 맹점 0, **패키지 안 venv로 sys.executable 수정 실전 증명**)→데모 HTTP 200. 전 항목 통과.
- [x] zip 생성 (2026-07-19) — `~/Desktop/탐지분석계층-보고-2026-07-19.zip` (175KB)
- [x] 조립본을 `30-References/탐지분석계층-보고-<날짜>/`에 배치 (2026-07-19) — vault 안에서 발송 내용 확인 가능, build.py 기본 출력도 이 위치로 변경. zip은 `python -m zipfile`로 생성 (**`zip -r` 금지** — 한글명 zip을 윈도우 내장 해제기가 못 여는 문제 실측, Python zipfile=UTF-8 EFS+슬래시는 맥·윈도우 양쪽 통과)
- [ ] 발송 → 발송 스냅샷 폴더를 `99-Archive/`로 이동 (발송은 사용자 실행)

## DoD (완료 기준)

- 조립본이 맥·윈도우 안내 문서만으로 전 단계(OCR 포함) 실행 가능
- 발송 스냅샷이 날짜명으로 아카이브에 남아 발송 내용이 추적 가능

## 위험·대응

| 위험 | 대응 |
|---|---|
| paddle wheel OS별 차이 (리눅스/윈도우/맥) | ocr-env 격리라 본 환경 무피해, 에러 유형별 우회 |
| NFD 한글 폴더명 (맥↔윈도우 왕복 시 체크아웃 유실 관찰됨) | 새 산출물 폴더명 ASCII 우선 검토, zip 발송 |
| 발송본 불명 재발 | Step 5·6의 날짜 명명 + 발송 스냅샷 아카이브 의무화 |
| requirements 버전 함정 (3.10/3.11 설치 실패) | Step 2 표기 수정 + README 설치 검증 명령 유지 |
